import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("potato"));
// <App /> : component라고 부름. HTML을 반환하는 함수!
// react는 component와 함께 동작함, component는 대문자로 시작
